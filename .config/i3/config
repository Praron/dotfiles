# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
# font pango: Monospace 10
# font pango: SCP 9
font pango: Ubuntu Mono 12
# font pango: Fira Mono 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

force_focus_wrapping no
focus_wrapping no

# exec twmnd &
# Notification manager daemon
exec --no-startup-id dunst
exec --no-startup-id unclutter --timeout 2 --jitter 5 --ignore-scrolling

# Clipboard manager daemon
exec --no-startup-id clipmenud
bindsym $mod+c exec --no-startup-id clipmenu

# Autolock after idle
exec --nostartup-id xautolock -time 30 -locker "systemctl suspend" -detectsleep


bindsym $mod+F11 exec --no-startup-id xrandr --output LVDS-1 --auto --output VGA-1 --off && pkill dunst && dunst
bindsym $mod+F12 exec --no-startup-id xrandr --output VGA-1 --auto --output LVDS-1 --off && pkill dunst && dunst
bindsym $mod+Shift+F12 exec --no-startup-id xrandr --output VGA-1 --rotate normal --auto --right-of LVDS-1

# Translate-to-Russian script
bindsym $mod+o exec --no-startup-id ~/Scripts/dmenu_translate_notification.sh
bindsym $mod+Shift+o exec --no-startup-id notify-send "$(xsel -ob)" "$(trans -b :ru "$(xsel -ob)" | sed 's/ null//g')"
bindsym $mod+Ctrl+Shift+o exec --no-startup-id notify-send "$(xsel -op)" "$(trans -b :ru "$(xsel -op)" | sed 's/ null//g')"


bindsym $mod+Shift+t exec --no-startup-id ~/Scripts/dmenu_timer.sh

# Change Thinkpad trackpoint scroll emulation mode
bindsym $mod+F8 exec --no-startup-id ~/Scripts/trackpoint_mode_toggle.sh


# Start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec ~/Scripts/urxvtc_or_urxvtd.sh
#bindsym $mod+Shift+Return exec --no-startup-id samedir.sh

# Start text editor
bindsym $mod+Shift+p exec ~/Scripts/urxvtc_or_urxvtd.sh -e micro

# Start a FM
bindsym $mod+p exec pcmanfm

bindsym $mod+Shift+y exec ~/Scripts/mplayer_youtube.sh
bindsym $mod+w exec ~/Scripts/dmenu_wifi_connect.sh

# Kill focused window
bindsym $mod+Shift+q kill
# bindsym $mod+Escape kill

# Kill workspace
bindsym $mod+Mod1+Shift+Escape focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, kill, workspace next


# Make the currently focused window a scratchpad
bindsym $mod+Shift+grave move scratchpad

# Show the first scratchpad window
bindsym $mod+grave scratchpad show


# start dmenu (a program launcher)
bindsym $mod+Shift+d exec --no-startup-id dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

default_orientation vertical
hide_edge_borders both
new_window pixel 3

#bindsym $mod+Ctrl+t border toggle
bindsym $mod+Ctrl+Shift+b border none
# use no window title and a thick border
#bindsym $mod+Shift+t border normal 2

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

workspace 1 output LVDS-1
workspace 2 output LVDS-1
workspace 3 output LVDS-1
workspace 4 output LVDS-1
workspace 5 output LVDS-1
workspace 6 output VGA-1
workspace 7 output VGA-1
workspace 8 output VGA-1
workspace 9 output VGA-1
workspace 10 output VGA-1


# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
#        bindsym Left resize shrink width 10 px or 10 ppt
#        bindsym Up resize grow height 10 px or 10 ppt
#        bindsym Down resize shrink height 10 px or 10 ppt
#        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Left resize grow left 5 px or 5 ppt
        bindsym Up resize grow up 5 px or 5 ppt
        bindsym Down resize grow down 5 px or 5 ppt
        bindsym Right resize grow right 5 px or 5 ppt

        bindsym Shift+Left resize shrink right 5 px or 5 ppt
        bindsym Shift+Up resize shrink down 5 px or 5 ppt
        bindsym Shift+Down resize shrink up 5 px or 5 ppt
        bindsym Shift+Right resize shrink left 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Volume control
#bindsym XF86AudioRaiseVolume exec amixer -q set Master 5%+ ; exec killall -USR1 i3status
#bindsym XF86AudioLowerVolume exec amixer -q set Master 5%- ; exec killall -USR1 i3status
#bindsym $mod+Shift+F3 exec amixer -q set Master 20%+ ; exec killall -USR1 i3status
#bindsym $mod+Shift+F2 exec amixer -q set Master 20%- ; exec killall -USR1 i3status
#bindsym XF86AudioMute exec amixer -q set Master toggle


# Exit/suspend/restart
mode  "(s)leep (h)ybernate (p)owersave (r)estart shut(d)own (e)xit" {

        bindsym s exec sudo pm-suspend; mode "default"
        bindsym h exec sudo pm-hibernate; mode "default"
        bindsym p exec sudo pm-powersave; mode "default"
        bindsym d exec sudo 'shutdown -h now'; mode "default"
        bindsym r exec sudo 'shutdown -r now'; mode "default"
        bindsym e exit; mode "default"

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
# bindsym $mod+Shift+Delete mode "(s)leep (h)ybernate (p)owersave (r)estart shut(d)own (e)xit"

bindsym $mod+Shift+Delete exec i3-msg exit

# Background image
exec --no-startup-id ~/.fehbg

# Network app in tray
#exec --no-startup-id nm-applet

# exec /usr/bin/unity-settings-daemon

#exec setxkbmap -layout 'us,ru' -option 'grp:alt_shift_toggle,grp_led:scroll,numpad:microsoft,compose:caps'
# exec setxkbmap -option 'caps:escape'


# Rename workspace
#bindsym $mod+n exec /home/escapsit/.i3/rename_workspace

# Cmus find and open track
# bindsym $mod+Ctrl+j exec /home/escapsit/.i3/cmus_find_track

# Screen brightness controls
#bindsym XF86MonBrightnessUp exec xbacklight -inc 10
#bindsym XF86MonBrightnessDown exec xbacklight -dec 10

# Switch workspaces
bindsym $mod+Shift+Tab workspace prev
bindsym $mod+Tab workspace next


# Change keyboard layouts
#bindsym Alt_R exec "setxkbmap -layout 'us'" ; exec killall -USR1 i3status
#bindsym Shift+Alt_R exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status
#bindsym Ctrl+Alt_R exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status
#bindsym $mod+Escape exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status
# bindsym Mod1+Escape exec "setxkbmap -layout 'us'" ; exec killall -USR1 i3status
# bindsym $mod+Shift+Escape exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status
#bindsym Print exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status

#bindsym $mod+Ctrl+Z exec "setxkbmap -layout 'ru'" ; exec killall -USR1 i3status


# bindsym $mod+F1 exec "setxkbmap -layout 'ru'"
# bindsym $mod+F2 exec --no-startup-id xrandr --output VGA-2 --off

# Screenshot
bindsym Print exec --no-startup-id scrot '%Y-%m-%d-%H-%M-%S.png' -e 'mv $f ~/Screenshots/'
bindsym --release Shift+Print exec --no-startup-id import ~/Screenshots/$(date +%Y-%m-%d-%H-%M-%S).png

bindsym $mod+F9 exec --no-startup-id feh --no-xinerama $(ls -dp ~/Screenshots/* | grep -v /$ | sort -nr | head -1)

#bindsym --release Ctrl+Shift+print exec --no-startup-id ~/Scripts/record_gif.sh
bindsym --release Ctrl+Shift+Print exec --no-startup-id killall -s SIGTERM ffmpeg || ~/Scripts/record_gif.sh


# Cmus control
bindsym Ctrl+Home exec --no-startup-id cmus-remote -r ; exec --no-startup-id killall -USR1 i3status # prev
bindsym Ctrl+End exec --no-startup-id cmus-remote -u ; exec --no-startup-id killall -USR1 i3status # pause/play
bindsym Ctrl+Shift+End exec --no-startup-id cmus-remote -p # replay
bindsym Ctrl+Insert exec --no-startup-id cmus-remote -n ; exec --no-startup-id killall -USR1 i3status # next
bindsym Ctrl+Shift+Home exec --no-startup-id cmus-remote -k -3 # seek-
bindsym Ctrl+Shift+Insert exec --no-startup-id cmus-remote -k +3 # seek+

#bindsym XF86AudioPrev exec --no-startup-id cmus-remote -r #; pkill -RTMIN+1 i3blocks
#bindsym XF86AudioNext exec --no-startup-id cmus-remote -n #; pkill -RTMIN+1 i3blocks
#bindsym Shift+XF86AudioPrev exec --no-startup-id cmus-remote -l -3 #; pkill -RTMIN+1 i3blocks
#bindsym Shift+XF86AudioNext exec --no-startup-id cmus-remote -k +3 #; pkill -RTMIN+1 i3blocks
#bindsym XF86AudioPlay exec --no-startup-id "cmus-remote -u" #; pkill -RTMIN+1 i3blocks
#bindsym Shift+XF86AudioPlay exec --no-startup-id cmus-remote -p #; pkill -RTMIN+1 i3blocks


#Lock screen
#bindsym $mod+Control+l exec --no-startup-id i3lock -d -i /home/escapsit/Downloads/vocaloid-anime-1366x738-wallpaper.png
bindsym --release $mod+Control+l exec --no-startup-id xdotool key Alt_R ; exec --no-startup-id ~/Scripts/screen_lock.sh ; exec xset dpms force off
bindsym --release XF86AudioMicMute exec --no-startup-id xdotool key Alt_R ; exec --no-startup-id ~/Scripts/screen_lock.sh ; exec xset dpms force off

# Colors
# class                 border  bground text    indicator child_border
client.focused          #6272A4 #6272A4 #282a36 #6272A4   #6272A4
client.focused_inactive #44475A #44475A #282a36 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36
client.background #F8F8F2


#client.focused #ff5555 #ff5555 #282a36
#client.unfocused #282a36 #282a36 #6272a4
#client.focused_inactive #555555 #555555 #111111
#client.urgent #383a3b #383a3b #ee0000


#client.focused #c5291b #c5291b #111111
#client.unfocused #252525 #2e2e2e #bbbbbb
#client.focused_inactive #555555 #555555 #111111
#client.urgent #383a3b #383a3b #ee0000

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        output LVDS-1
        # colors { # Dracula theme
        #     background #282A36
        #     statusline #F8F8F2
        #     separator  #44475A

        #     focused_workspace  #44475A #44475A #F8F8F2
        #     active_workspace   #282A36 #44475A #F8F8F2
        #     inactive_workspace #282A36 #282A36 #BFBFBF
        #     urgent_workspace   #FF5555 #FF5555 #F8F8F2
        #     binding_mode       #FF5555 #FF5555 #F8F8F2
        # }

        colors {
        	# Whole color settings background #000000 statusline #ffffff separator #666666
        	statusline #dddddd

	        # Type             border  background font
            focused_workspace  #44475A #44475A #000000
    	    # focused_workspace  #777777 #777777 #000000
            active_workspace   #333333 #333333 #000000
       	    inactive_workspace #000000 #000000 #bbbbbb
       	    urgent_workspace   #aa0000 #990000 #ffffff
        }

        tray_output none

        position top
        status_command i3blocks
}


bar {
        output VGA-1

        # colors { # Dracula theme
        #     background #282A36
        #     statusline #F8F8F2
        #     separator  #44475A

        #     focused_workspace  #44475A #44475A #F8F8F2
        #     active_workspace   #282A36 #44475A #F8F8F2
        #     inactive_workspace #282A36 #282A36 #BFBFBF
        #     urgent_workspace   #FF5555 #FF5555 #F8F8F2
        #     binding_mode       #FF5555 #FF5555 #F8F8F2
        # }

        colors {
            # Whole color settings background #000000 statusline #ffffff separator #666666
            statusline #dddddd

            # Type             border  background font
            focused_workspace  #44475A #44475A #000000
            active_workspace   #333333 #333333 #000000
            # focused_workspace  #777777 #777777 #000000
            # active_workspace   #444444 #444444 #000000
            inactive_workspace #000000 #000000 #bbbbbb
            urgent_workspace   #aa0000 #990000 #ffffff
        }

        tray_output none

        position bottom
        status_command i3blocks
}
